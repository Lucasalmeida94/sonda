{
  "name": "sonda",
  "version": "0.1.0",
  "private": true,
  "description": "Protótipo de radar de dispositivos eletrônicos (sinais simulados) empacotado como app Android",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
